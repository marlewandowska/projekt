import React from "react";
import { useNavigate } from "react-router-dom";

export interface ICosProps { }

const Cos: React.FunctionComponent<ICosProps> = (props) => {

    return (
        <div className="cos">

        </div>
    );
}

export default Cos;